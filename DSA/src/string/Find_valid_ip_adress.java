package string;

import java.util.Scanner;
import java.util.regex.Pattern;

public class Find_valid_ip_adress {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		String ip = "";
		Pattern p = Pattern.compile("[0-9]{3}[");
		while(sc.hasNext()) {
			ip = sc.nextLine();
			
		}
		
	}
}
